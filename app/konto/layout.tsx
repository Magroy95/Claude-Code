// Das Konto ist immer nutzerspezifisch – kein Zwischenspeichern.
export const dynamic = "force-dynamic";

export default function KontoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
